package com.trading.ctrm.lifestyle;

public enum RejectionReason {
    NO_RULE_FOUND,
    RULE_DISABLED,
    MAX_OCCURRENCE_EXCEEDED,
    INVALID_STATE,
    DESK_MISMATCH,
    NOT_EFFECTIVE_DATE,
    HARD_BLOCK
}
